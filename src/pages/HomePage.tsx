import { SectionContainer } from '../components';

// TODO - pass home components to features
export const HomePage = () => {
  return (
    <SectionContainer>
      home component
    </SectionContainer>
  );
};
