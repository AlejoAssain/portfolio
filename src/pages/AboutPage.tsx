import { SectionContainer } from '../components';
import { About } from '../features/About';

export const AboutPage = () => {
  return (
    <SectionContainer>
      <About />
    </SectionContainer>
  );
};
